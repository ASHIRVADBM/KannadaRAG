# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Only needed for the optional hosted-model comparison. The default pipeline
# runs entirely locally through Ollama and requires no API key at all.
GROQ_API_KEY=

# Ollama endpoint for local inference.
OLLAMA_HOST=http://localhost:11434
